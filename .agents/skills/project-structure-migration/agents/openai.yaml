interface:
  display_name: "Project Structure Migration"
  short_description: "Guide safe file-only architecture migrations"
  default_prompt: "Use $project-structure-migration to reorganize this codebase into app/core/shared folders without changing behavior or business logic."
